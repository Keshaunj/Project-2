<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cars Index Page</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
</head>

<body>
  <%- include('../partials/nav') %>

  <div class="index-header-div">
    <h1>All Cars</h1>
    <a class="add-Car" href="/Car/new">Add Car</a>
  </div>

  <ul>
    <% Car.forEach((Car)=> { %>
      <li>
        <a class="Car-cards" href="/Car/<%=Car._id%>">
          <%= Car.name %>
        </a> 
        | <a href="/Car/<%= Car._id %>/edit">Edit</a>
        | <form action="/Car/<%= Car._id %>?_method=DELETE" method="POST" style="display:inline;">
            <button type="submit">Delete</button>
          </form>
      </li>
    <% }) %>
  </ul>

  <a href="/">Go Back to Homepage</a>

</body>
</html>
